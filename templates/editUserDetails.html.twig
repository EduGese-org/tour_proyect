{% extends 'base.html.twig' %}

{% block body %}

<div class="  e-frm frm col-lg-10 mr-auto ml-auto">
   <h2 class="">Edita tus datos</h2>
    {{ form_start(user_edit_form) }}
    
    <div>
        {{ form_label(user_edit_form.user_name, 'Nombre de usuario') }}
        {{ form_widget(user_edit_form.user_name) }}
    </div>
    
    <div>
        {{ form_label(user_edit_form.user_lastname, 'Apellidos') }}
        {{ form_widget(user_edit_form.user_lastname) }}
    </div>
    
    <div>
        {{ form_label(user_edit_form.tel, 'Teléfono') }}
        {{ form_widget(user_edit_form.tel) }}
    </div>
    
    <div>
        {{ form_label(user_edit_form.email, 'Email') }}
        {{ form_widget(user_edit_form.email) }}
    </div>
    
    <button type="submit">Guardar cambios</button>
    {{ form_end(user_edit_form) }}
</div>
    
{% endblock %}